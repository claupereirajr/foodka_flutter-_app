import 'package:flutter/material.dart';

class RecipeInstruction extends StatelessWidget {
  const RecipeInstruction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}